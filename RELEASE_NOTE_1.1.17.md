# Release note 1.1.17

Этот файл относится только к отдельному release-снимку.

## Почему выпущена версия `1.1.17`

- изменения для публикации выполнялись именно в release-снимке
- нужно явно отделить release-сборку от основной рабочей копии проекта
- версия `1.1.16` уже была опубликована в `npm`, поэтому для новой release-публикации нужен новый номер

## Что отмечено этой версией

- `node-red-contrib-golc-alice@1.1.17` — release-only отметка
- причина повышения версии записана в release-снимке, а не в основном проекте

## Важно

- основной рабочий проект не используется как место для этой release-пометки
- эта note нужна, чтобы было понятно, где именно делались релизные изменения